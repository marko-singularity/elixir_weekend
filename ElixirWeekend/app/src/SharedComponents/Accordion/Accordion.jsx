import React from 'react';
import { Accordion as MaterialAccordion } from '@material-ui/core';

const Accordion = (props) => {
  return <MaterialAccordion {...props} />;
};

export default Accordion;
